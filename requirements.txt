# GestureWave AI — Python dependencies
# Install with: pip install -r requirements.txt

# Core hand tracking
mediapipe==0.10.5
opencv-python==4.8.1.78

# Input simulation
PyAutoGUI==0.9.54

# Math / array processing
numpy>=1.24.0,<2.0.0

# Database & Auth
supabase>=2.0.0
python-dotenv>=1.0.0

# Optional: GUI launcher
# (tkinter ships with standard Python — no pip install needed)

# Optional: packaging to .exe
# pyinstaller>=6.0
